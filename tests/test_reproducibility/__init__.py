"""
Reproducibility test module for Snake-RL

Tests to verify reproducibility across:
- Same seed produces same results
- Same device produces identical runs
- Cross-device produces results within tolerance
"""
